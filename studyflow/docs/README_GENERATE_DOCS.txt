Doxygen is not installed in this workspace, so the HTML pages were not generated here.

Before you submit:
1. Install Doxygen on your machine.
2. Open the project root.
3. Run:
   doxygen dox.config
4. Confirm that docs/html/index.html exists.

Then submit the project folder with the generated docs/html directory included.
